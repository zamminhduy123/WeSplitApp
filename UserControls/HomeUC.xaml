<UserControl x:Class="WeSplitApp.UserControls.HomeUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeSplitApp.UserControls"
             
             
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
         TextElement.FontWeight="Regular"
         TextElement.FontSize="13"
         TextOptions.TextFormattingMode="Ideal"
         TextOptions.TextRenderingMode="Auto"
         FontFamily="{DynamicResource MaterialDesignFont}"
             Background="White"
             DataContext="{StaticResource HUCVM}"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="2080">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/> 
            <RowDefinition/> 
            <RowDefinition/> 
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- thumbnail image -->
        <Border Grid.Column="0"  VerticalAlignment="Bottom" Grid.Row="0" Grid.RowSpan="2" BorderThickness="3" BorderBrush="Black"
                        CornerRadius="50" Margin="10" Height="430" Padding="0" RenderOptions.BitmapScalingMode="HighQuality">
            <Border.Background>
                <ImageBrush Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality" ImageSource="/Assets/du-lich-ha-long-1.jpeg">
                </ImageBrush>
            </Border.Background>
        </Border>
        <!-- member list -->
        <Border Grid.Column="0"  VerticalAlignment="Bottom" Grid.Row="2" Grid.RowSpan="2" BorderThickness="5" BorderBrush="Black"
                        CornerRadius="50" Height="450" Padding="0" RenderOptions.BitmapScalingMode="HighQuality" Margin="5">
            <StackPanel>
                <Border  CornerRadius="50"  BorderThickness="2" BorderBrush="Black" Height="100" Background="#03254c" >
                    <TextBlock Text="THÀNH VIÊN THAM GIA" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" FontWeight="Medium"></TextBlock>
                </Border>
                <ListView ItemsSource="{Binding ParticipantsList}">
                    <ListView.ItemTemplate >
                        <DataTemplate>
                            <Grid Margin="0" Width="100" >
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}" FontSize="12   " Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                    <TextBlock Text="{Binding Quantity}" FontSize="10" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>

        <!-- date -->
        <Border Grid.Column="1" Grid.Row="0" Background="#d0efff" CornerRadius="50" Margin="10" >
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" Margin="0 30 0 0" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="Calendar" Width="60" Height="60" />
                    <TextBlock Margin="10 0 0 0" Foreground="Red" Text="NGÀY ĐI: 0/0/0000" FontSize="40" VerticalAlignment="Center" FontWeight="Medium"> </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 20 0 0" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="Calendar" Width="60" Height="60" />
                    <TextBlock Margin="10 0 0 0" Foreground="#03254c" Text="NGÀY ĐI: 0/0/0000" FontSize="40" VerticalAlignment="Center" FontWeight="Medium"></TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- route list -->
        <Border Grid.Column="1" Grid.Row="1" Grid.RowSpan="3" Background="white" CornerRadius="50" Margin="10" BorderThickness="5" BorderBrush="Black" >
            <StackPanel>
                <Border  CornerRadius="50"  BorderThickness="2" BorderBrush="Black" Height="100" Background="#03254c" >
                    <TextBlock Text="THÀNH VIÊN THAM GIA" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" FontWeight="Medium"></TextBlock>
                </Border>
                <ListView ItemsSource="{Binding RoutesList}">
                    <ListView.ItemTemplate >
                        <DataTemplate>
                            <Grid Margin="0" Width="100" >
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}" FontSize="12   " Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                    <TextBlock Text="{Binding Quantity}" FontSize="10" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
        
        <!-- Summary -->
        <Border Grid.Column="3" Grid.Row="0" Grid.RowSpan="4" Background="white" CornerRadius="50" Margin="10" BorderThickness="5" BorderBrush="Black" >
            <StackPanel>
                <Border  CornerRadius="50"  BorderThickness="2" BorderBrush="Black" Height="100" Background="#03254c" >
                    <TextBlock Text="TỔNG KẾT" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" FontWeight="Medium"></TextBlock>
                </Border>
                <ListView ItemsSource="{Binding SummaryList}">
                    <ListView.ItemTemplate >
                        <DataTemplate>
                            <Grid Margin="0" Width="100" >
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}" FontSize="12   " Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                    <TextBlock Text="{Binding Quantity}" FontSize="10" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
